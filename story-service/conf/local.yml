server :
  port : "464"
  host : "localhost"
  secret : "MagyGram"
  name: "story-service"
  handshake : "ms-handshake"

database :
  host : "mongodb://localhost"
  port : "27017"
  user : "root"
  password : "root"
  database : "no-waiter-story-service"

mail :
  sender : "info.magygram@gmail.com"
  password : "magygramxml"
  host : "smtp.gmail.com"
  port : "587"

mediaservice:
  protocol: "https://"
  domain: "localhost"
  port: "465"

authservice:
  protocol: "https://"
  domain: "localhost"
  port: "461"

userservice:
  protocol: "https://"
  domain: "localhost"
  port: "460"

relationshipservice:
  protocol: "https://"
  domain: "localhost"
  port: "463"
